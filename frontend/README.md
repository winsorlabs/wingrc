# WinGRC frontend (stub)

React 19 + Vite SPA. Currently a pilot shell that confirms the API is up and
lists the CMMC views the scope graph can project. Build out the scope browser,
import/reconcile UI, and bundle generation from here.

```bash
npm install
npm run dev   # http://localhost:5173  (proxies /api -> backend:8000)
```
